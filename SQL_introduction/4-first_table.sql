-- Creates a table called first_table in the current database
-- Creates the table first_table if it doesn't exist
CREATE TABLE IF NOT EXISTS first_table (id INT DEFAULT NULL, name VARCHAR(256) DEFAULT NULL);
