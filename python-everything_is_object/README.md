# python everything is object

## 0-answer to 28-answer
text files containing the answer to the task questions

## 19-copy_list
a function that returns a copy of a list

## 100-magic_string
a function that returns a string "BestSchool" n times

## 101-locked_class
a class LockedClass that prevents the user from dynamically creating new instance attributes, except if the new instance attribute is called first_name
