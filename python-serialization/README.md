# python serialization

## task_00_basic_serialization
a module that serializes a Python dictionary to a JSON file and deserializes the JSON file to recreate the dictionary

## task_01_pickle
a module that serializes and deserializes custom Python objects using the pickle module

## task_02_csv
a module that reads data from a CSV file and converts it into a JSON format

## task_03_xml
a module that serializes a Python dictionary to XML and deserializes the XML file to recreate the dictionary
