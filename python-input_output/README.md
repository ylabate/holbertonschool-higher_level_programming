# python input output

## 0-read_file
a function that reads a text file and prints it to stdout

## 1-write_file
a function that writes a string to a text file and returns the number of characters written

## 2-append_write
a function that appends a string at the end of a text file and returns the number of characters added

## 3-to_json_string
a function that returns the JSON representation of an object as a string

## 4-from_json_string
a function that returns an object represented by a JSON string

## 5-save_to_json_file
a function that writes an object to a text file using JSON representation

## 6-load_from_json_file
a function that creates an object from a JSON file

## 7-add_item
a script that adds all arguments to a Python list and saves them to a JSON file

## 8-class_to_json
a function that returns the dictionary description for JSON serialization of an object

## 9-student
a class Student that defines a student with a method to retrieve its dictionary representation

## 10-student
a class Student with a method to_json that retrieves a dictionary representation with optional attribute filtering

## 11-student
a class Student with methods to convert to JSON and reload from a JSON dictionary

## 12-pascal_triangle
a function that returns a list of lists representing Pascal's triangle of n rows
